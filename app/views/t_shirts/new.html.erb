<div class="main t_shirts-new">
  <div class="container">
    <h1 class="form-heading">追加する</h1>
    <%= form_tag("/t_shirts/create",{multipart: true}) do %>
      <div class="form">
        <div class="form-body">
          <% @t_shirt.errors.full_messages.each do |message| %>
            <div class="form-error">
              <%= message %>
            </div>
          <% end %>
          <p>ブランド</p>
          <textarea name="content"><%= @t_shirt.content %></textarea>
          <p>色</p>
          <textarea name="color"><%= @t_shirt.color %></textarea>
          <p>サイズ</p>
          <textarea name="size"><%= @t_shirt.size %></textarea>
          <input type="submit" value="追加">
        </div>
      </div>
    <% end %>  
  </div>
</div>